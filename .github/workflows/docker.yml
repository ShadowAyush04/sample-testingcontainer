name: Build Docker Image

on:
  push:
    # Trigger on your master branch
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # --- 🏙️ ANTIGRAVITY OPTIMIZATION GATE START ---
      - name: Antigravity Optimization Gate
        env:
          # 🟢 CHOICE: 'true' to use AI-fixed Dockerfile, 'false' for original
          USE_OPTIMIZED: 'true' 
          OPTIMIZER_SERVER: 'http://40.65.144.231:8000/api'
        run: |
          # 1. Download the tool (Including the subfolder prefix)
          curl -sSL https://raw.githubusercontent.com/codeayush004/techdemoversion1/main/container-optimizer/bin/optimizer-cli.py -o optimizer-cli.py
          
          # 2. Permission Logic
          if [ "$USE_OPTIMIZED" = "true" ]; then
            echo "✅ Permission Granted: Swapping with Optimized Dockerfile"
            python3 optimizer-cli.py --file Dockerfile --server "$OPTIMIZER_SERVER" --apply
          else
            echo "🐢 Using Original Dockerfile (Scanning for Criticals only)"
            python3 optimizer-cli.py --file Dockerfile --server "$OPTIMIZER_SERVER" --fail-on CRITICAL
          fi
      # --- 🏙️ ANTIGRAVITY OPTIMIZATION GATE END ---

      - name: Build Docker image
        run: |
          # This builds the AI-fixed version IF USE_OPTIMIZED was true
          docker build -t bad-docker-app .